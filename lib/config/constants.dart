class AppConstants {
  AppConstants._();

  static const String appName = 'Food Aid';
  static const String appTagline = 'Share food, spread hope';

  static const Duration splashDuration = Duration(milliseconds: 2000);
  static const Duration skeletonMinDuration = Duration(milliseconds: 800);
  static const Duration animationFast = Duration(milliseconds: 200);
  static const Duration animationMedium = Duration(milliseconds: 350);
  static const Duration animationSlow = Duration(milliseconds: 500);

  static const double scaleTapFactor = 0.98;
  static const double cardHoverElevation = 4.0;

  static const int paginationLimit = 20;
}
